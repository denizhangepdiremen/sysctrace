/*
 * sysctrace.h — public interface for the syscall tracer
 */
#ifndef SYSCTRACE_H
#define SYSCTRACE_H

#include <stdint.h>
#include <sys/types.h>

/* Project version */
#define SYSCTRACE_VERSION "0.1.0"

/* Forward declarations — will be filled in later phases */
typedef struct trace_context trace_context_t;

#endif /* SYSCTRACE_H */